import React from 'react';

const Test = (props) => {
    console.log(props.cart);
    return (
        <div>
            <h1>Hello this is Test state</h1>
        </div>
    );
};

export default Test;